# IMPLEMENTATION STATUS

Live status of every major subsystem. Updated each phase.

Legend: IMPLEMENTED | PARTIAL | BLOCKED_EXTERNAL | PLANNED | EXPERIMENTAL

| Subsystem | Crate / Dir | Status | Notes |
|-----------|-------------|--------|-------|
| Core errors/ids/time | ald-core | IMPLEMENTED | error enum (+Network), AldivinePlayerId (ULID), EntityId (generation), time |
| Config + validation | ald-config | IMPLEMENTED | server/network/identity/security sections, strict validate() |
| Protocol framing | ald-protocol | IMPLEMENTED | 32-byte header, 9 channels, flags, encode/decode, event codec |
| Citizen serialization | crates/ald-serialization-compat | IMPLEMENTED | hand-written msgpack encoder/decoder matching CitizenFX wire: ext 20/21/22/23 (vec2/3/4, quat) big-endian f32, ext 10/11 funcref preserved as opaque, nil vs false distinct; byte-exact golden tests against reference msgpack output; 15 tests |
| Device identity | ald-device-identity | IMPLEMENTED | keyed HMAC fingerprint, confidence, versioning |
| Identity service | ald-identity | IMPLEMENTED | PlayerIdentity, Steam hex (correct formula), providers, ban match confidence |
| Event system | ald-events | IMPLEMENTED | namespaced, priority, protected namespaces, Arc handlers, no-deadlock dispatch |
| RPC | ald-rpc | IMPLEMENTED | request IDs, timeouts, pending tracking, expire() |
| Resource system | ald-resource | IMPLEMENTED | manifest parse, lifecycle+leak detect, capabilities, dep resolution |
| Permissions (RBAC) | ald-permissions | IMPLEMENTED | roles + wildcard + granular per-principal grants |
| AstraNet transport | ald-network | IMPLEMENTED | UDP transport, session manager + idle expiry, reliability (replay/dup/order, 32-bit wrap), token-bucket rate limiter, event firewall (direction/size/permission/channel ACL), IP normalize + trusted-proxy CIDR, fragmentation |
| Scheduler | ald-scheduler | IMPLEMENTED | workload classes, budgets, runaway detector, async executor lanes (ordered/parallel/background) with live stats |
| ECS entities | ald-ecs | IMPLEMENTED | slot-based EntityId generation, spawn/despawn, component map, ownership CAS transfer (server delegates, lateral handoffs refused, stale-safe), dimensions (default-0, strict same-plane visibility, ghost-proof assignment, prune backstop, proven interest-set composition); 26 tests |
| Security utils | ald-security | IMPLEMENTED | secret redaction, log sanitization |
| Telemetry | ald-telemetry | IMPLEMENTED | metrics counters/histograms |
| Audit | ald-audit | IMPLEMENTED | structured audit events |
|| Database layer | ald-database | IMPLEMENTED | repository trait, pool config, query timing, slow-query detection, RAII transactions with rollback-on-drop, canonical money-transfer tx, deterministic migration runner with drift detection |
|| Entitlement service | crates/ald-entitlement | IMPLEMENTED | entitlement models (FREE/PAID/SUBSCRIPTION/PRIVATE/DEVELOPER/BETA/INVITE_ONLY), server registration with signed credentials, package entitlement resolution; 13 tests |
|| Package format + signing | crates/ald-package | IMPLEMENTED | .alpkg manifest, Ed25519 signing over canonical bytes, SHA-256 payload verification, OPEN/SIGNED/PROTECTED/PRIVATE policies, tamper + wrong-key + stale-hash rejection; 10 tests |
|| Login pipeline | crates/ald-identity/src/pipeline.rs | IMPLEMENTED | identity requirement policy, structured ALD-* connection codes, Epic-owner-not-blocked rule, trusted-proxy IP resolution; 13 tests |
|| Compat abstraction | ald-compat | PARTIAL | ACL trait; ESX/QBCore/Qbox adapters PLANNED |
| Lua runtime | crates/ald-script-lua | IMPLEMENTED | mlua 0.10 Lua 5.4 vendored; sandbox removes io/os/package/debug/jit/require/loadfile; 5 tests |
| JS runtime | crates/ald-script-js | IMPLEMENTED | rquickjs 0.13 QuickJS bindgen; Citizen-compatible client surface (setTimeout/setInterval/clear, Citizen.Wait/CreateThread/SetTimeout, on/onNet/emit, TriggerClientEvent/TriggerServerEvent onto host-drained wire queue, console to host logs, GetCurrentResourceName), virtual-clock timer pump (tick_timers), callbacks kept in Rust-side registries as GC-rooted Persistent handles (JS heap carries functions only, no data), calling context passed as callback parameter (never captured — a stored Ctx clone deadlocks teardown via DupContext refcount and trips the debug GC-empty assert), explicit Drop frees roots while heap is live, field order state/ctx/runtime; require/process/Buffer absent by design (ReferenceError, honest failure); 16 tests |
| Server runtime | server/ald-server | IMPLEMENTED | config-validated startup, AstraNet listener, LifecycleSupervisor (single-writer start/stop/restart via channel, leak detection), console with audited commands + remote command channel (Aegis/operator injection via console_tx, served alongside stdin, exits on shutdown), console Stop triggers real shutdown via request_shutdown, telemetry loop, graceful shutdown with SIGTERM/Ctrl-C; 9 tests |
| Replication | crates/ald-replication | IMPLEMENTED | baseline+delta, tier-driven send rate, coalescing, queue cap/drop with stats; 6 tests |
| Spatial partition + interest | crates/ald-ecs/src/spatial.rs | IMPLEMENTED | uniform grid, radius query, interest tiers (20/10/2 Hz), replication set excludes self + far; 8 tests |
| Astryn client | client/astryn | IMPLEMENTED | lifecycle state machine, background connect task, bounded graceful shutdown; failures degrade to Failed not panic; 2 tests |
| Framework | framework/aldivine | IMPLEMENTED | economy (atomic transfer + ledger, money-supply conservation), inventory (weight/stack/nesting limits, rollback on failed transfer), jobs (grades, duty, salary, rank), players (characters, slot limit, unique names); 26 tests |
| Compatibility (ESX/QB/Qbox) | crates/acl-esx, acl-qbcore, acl-qbox | PARTIAL | LegacyAdapter trait; ESX 4 tests, QBCore 3, Qbox 2; read-only player projections over framework services, honest per-API coverage matrix in docs/compatibility/FIVEM_API_MATRIX.md; mutations server-context only; event/callback/export/command/state-bag adapters still PLANNED |
|| NovaGate | apps/novagate (novagate-core) | IMPLEMENTED | GTA V install detection (registry/Steam-library/Epic-manifest, decisive 3-file structure check), distribution classification, detection-vs-entitlement separation enforced by type, AldivinePaths layout (%LOCALAPPDATA%/Aldivine, never touches game install), signed update manifests (signature/channel/min-version/downgrade rejection, payload hash verification), server browser state (favorites, bounded dedup history, direct-connect validation); 31 tests |
|| Aegis | aegis/backend (aegis-core) | IMPLEMENTED | PBKDF2-HMAC-SHA256 password hashing (constant-time verify, random salt, never plaintext), RFC 6238 TOTP with skew window (HOTP core verified against RFC 4226 vectors 755224/287082), session store (absolute+idle TTL, immediate invalidation, GC), append-only audit log, brute-force lockout (per-account, audited), permission-gated warn/kick/ban with denied-action auditing, resource profiler (P50/P95/P99 nearest-rank, slow detection, bounded window), alerts (dedup, resolve, reconcile, webhooks), incident timeline correlation; 82 tests |
| Time synchronization | crates/ald-timesync | IMPLEMENTED | NTP-style offset/RTT estimator keeping lowest-RTT samples, jitter spread, clamped interpolation delay, server-authoritative monotonic + wall clock pair, client time-claim bounds rejecting stale/forged history (lag compensation never trusts client timestamps); 10 tests |
| Feature negotiation + deprecation UX | crates/ald-negotiation | IMPLEMENTED | server-authoritative Hello/feature negotiation with typed outcomes (Selected/ClientOnlyUnsupported/RequiresClientUpdate/RequiresServerUpdate/DisabledByPolicy), structured protocol deprecation (ClientTooOld/ServerTooOld/Incompatible) carrying UpgradeAction with channel + min version, strict required-feature hard-fail, capability intersection; 11 tests |
| Server query protocol | crates/ald-query | IMPLEMENTED | anonymous UDP probe ("ALDQ" magic, strict length/magic/version validation), bounded native JSON response with progressive shedding (players -> icon -> extra), unlisted servers answer counts only, per-source rate limiter with GC (anti-amplification), legacy /info.json /players.json /dynamic.json derived from one ServerStatus; 13 tests |
| fxmanifest / manifest normalization | crates/ald-fxmanifest | IMPLEMENTED | hand-written Lua table-literal lexer + parser for fxmanifest.lua / __resource.lua (no Lua interpreter dependency), NormalizedManifest target, all documented directives parsed (fx_version, game(s), client/server/shared scripts, files, ui_page, exports, server_exports, dependency/provide, data_file, level meta, loadscreen, server_only, lua54, node_version, clr_disable_task_scheduler, use_experimental_fxv2_oal, convar_category), unknown directives classified UNSUPPORTED and preserved verbatim (never silently discarded), fileserver_add TRANSLATED to streaming/CDN origin, convar_category parsed into Aegis form-schema (convar/name/help/default/min/max/type), ald_manifest.toml also supported; 20 tests |
| Node compatibility runtime | crates/ald-script-node | PARTIAL | 33 tests: engine-independent half only — node16/node22 profiles with `node_version` mapping, `engines.node` semver check, `NodeFileSource`-backed require() resolution (extension probing, package.json main, node_modules walk, scoped names), lockfile-frozen deterministic planning, sha512/sha256 integrity parse+verify against independently known digests, content-address cache path, scripts-off/addons-blocked policy with arch check, 30-entry built-in capability table; JS evaluation never claimed. See docs/NODE_COMPATIBILITY.md |
| Dotnet compatibility runtime | crates/ald-script-dotnet | PARTIAL | 20 tests: load-and-authorize half only — dependency-free ECMA-335 reader over real on-disk assemblies (identity, refs, TypeDefs, P/Invokes, MVID), BCL target inference, Mono/CoreClr profiles from manifest `clr_disable_task_scheduler`, transitive load plans with downgrade/trust/cycle gates, ordered resource authorization (BCL/CitizenFX.Core/BaseScript/signature/native), virtual-clock TickPump; CIL execution never claimed. See docs/DOTNET_COMPATIBILITY.md |
| CfxLua compatibility frontend | crates/ald-cfxlua-compat | IMPLEMENTED | vector2/vector3/vector4/quaternion value types with Cfx field semantics + arithmetic metatables (__add/__sub/__mul vec*vec and vec*scalar, __tostring, __eq), joaat one-at-a-time hashing exposed as joaat() global and as a backtick source preprocessor (`adder` -> joaat("adder")) with reference values verified independently, json global with Cfx array-vs-object table detection, msgpack global with a self-contained ext-free msgpack codec (golden wire bytes for fixint/nil/bool/fixstr/empty arr+map, truncated-input rejected), promise global + Citizen.Await with synchronous resolve and reject-as-error; 20 tests |
| Aldivine VFS (virtual filesystem) | crates/ald-vfs | IMPLEMENTED | resource-scoped sandbox mount table with @resource/path resolution, cross-resource access denied unless explicitly shared, one-way share(), lexical traversal bound (net depth must never go negative), Windows reserved device-name rejection (COM1-9/LPT1-9, con/prn/aux/nul), alternate-data-stream rejection, NUL byte / RTLO / combining-mark rejection, write path uses temp-file + canonical re-verify + atomic rename, symlink escape blocked by canonical containment on every read and write; 16 tests incl. a 4600-case adversarial path fuzz sweep proving every generated path either errors or resolves inside the sandbox root |
| Aldivine Edge gateway | crates/ald-edge | IMPLEMENTED | pre-auth admission (per-IP + origin connection caps, banned IP/CIDR), query rate limiting isolated from gameplay budget, origin shielding (fail_closed + shield_allow_networks), HMAC-SHA256 authenticated real-IP forwarding with freshness window and constant-time compare, stdlib-only SHA-256 verified against FIPS 180-2 vectors and HMAC against RFC 4231 TC2; 15 tests |
| Native extension ABI | crates/ald-native-extension (+ testext fixture) | IMPLEMENTED | extension.toml manifest, HOST_ABI negotiation (mismatch is hard refusal), requires_runtime semver range, bare-filename library confinement, Ed25519 signature over canonical manifest bytes + lib sha256 with operator trust list (default-deny, unsigned refused unless opted in), known-capability allowlist with per-deploy grants, quarantine vs operational error classification, real dlopen/dlsym load of a compiled cdylib fixture exporting ald_extension_init (init 0 success, non-zero refusal, foreign-ABI failure); loaded code runs in-process, documented as trust gate not sandbox; 19 tests |
| Admission queue | crates/ald-queue | PARTIAL | playing slots, FIFO waitlist, reserved-slot rule, stale-entry expiry on caller-supplied virtual clock (deterministic, no I/O); 12 tests; consumption by the AstraNet handshake PLANNED |
| Pre-join deferrals | crates/ald-deferrals | PARTIAL | ordered non-blocking gates (Pass/Wait/Deny), poll watchdog denying stuck sessions with ALD-QUEUE-TIMEOUT, bounded player-facing messages, client-safe denial codes with gate attribution; 10 tests; handshake wiring PLANNED |
| Game editions | crates/ald-game-editions | IMPLEMENTED | Legacy/Enhanced enum, strict parse (nothing else parses, no Unknown variant), EditionProfile support/refusal; 4 tests |
| Game builds | crates/ald-game-builds | PARTIAL | data-driven registry, ordered assessment (edition/block/version), malformed data rejected at add, unparsable client versions fail closed, EmergencyPin opt-in fallback that never overrides known verdicts; 13 tests on synthetic 90000+ numbers; certified corpus BLOCKED_EXTERNAL (no GTA V on dev box) |
| State bags | crates/ald-state | IMPLEMENTED | typed values, per-bag seq versions, Open/OwnerOnly policy (no owner = nobody writes), key/value bounds, per-key replication flags, ordered change handlers that are observers by construction; 10 tests |
| State-bag compat | crates/ald-statebag-compat | IMPLEMENTED | GlobalState/entity/player/local scope naming with exact round-trip, entity/player id namespaces collision-free, compat key rules within core bounds, strict create/write/transfer ownership, no implicit creation on write path; 8 tests |
| Asset formats | crates/ald-asset-registry | IMPLEMENTED (recognition) | 11 spec-seeded extensions with stream classes, case-insensitive path/extension recognition, SpecSeed→Certified upgrade rule, duplicate/garbage rejection; 6 tests; content support PLANNED |
| DataFiles | crates/ald-datafiles | IMPLEMENTED (recognition) | vehicle-graph filename seed (5 entries), path/case normalization, unknown-is-None, per-category listing, ADS rejection with Windows drive-prefix allowance; 6 tests; other graphs PLANNED |
| Snapshots | crates/ald-snapshot | IMPLEMENTED | versioned checksummed framing (ALDSNAP1), truncation/corruption/version/garbage rejection with distinct errors; 9 tests |
| Persistence | crates/ald-persistence | IMPLEMENTED | append-only checksummed journal, atomic checkpoints with journal truncation, recovery distinguishing torn tail (drop+report) from mid-file corruption (fail) and torn checkpoint (ignore) from bad base (fail); sequences never reuse; 9 tests over real temp-dir files |
| Population | crates/ald-population | IMPLEMENTED | per-dimension ped/vehicle budgets with density-fraction caps (deterministic), fail-closed unconfigured dimensions, saturating releases, no retroactive cull on tighten, ordered creation hooks; 10 tests |
| Game events | crates/ald-game-events | PARTIAL | typed envelopes with spec catalog, wrap-aware per-producer dedup, ordered dispatch, decoder-slot resolution (exact/edition-fallback/unregistered) with evidence refs; payload decoding BLOCKED_EXTERNAL (no GTA on dev box); 9 tests |
| Lag compensation | crates/ald-lagcomp | IMPLEMENTED | bounded tick-ring position history (exact/lerp/None, no extrapolation), tick-domain rewind gate (future fails, budget enforced), horizontal-plane hit resolve matching interest convention; 8 tests |
| Download engine | crates/ald-download | PARTIAL | chunk plans with short-tail math, idempotent per-byte reassembly (partial never valid), resume shopping lists, virtual-tick stall watchdog (reports only), turn-based DRR across 5 lanes with exact weight shares; transport/CDN PLANNED; 10 tests |
| Asset cache | crates/ald-cache | IMPLEMENTED | content-addressed store with sharded layout, atomic hash-verified commits, structural dedup, verify/repair with removal reports, quota LRU (mtime-rebuilt) with mounted protection, stray-file immunity; 10 tests over real files |
| Asset mount | crates/ald-mount | IMPLEMENTED | gated Queued→Registered state machine (format/verify/dependency gates, illegal jumps rejected, terminal states stick); 8 tests |
| Asset residency | crates/ald-residency | IMPLEMENTED | virtual-tick Requested→Ready handshake (bounded retries, expiry, fatal handling, bridge called only on request/retry, terminal hygiene); real GameCommit BLOCKED_EXTERNAL; 8 tests |
| Streaming orchestrator | crates/ald-streaming | IMPLEMENTED | unified asset states, join gate, byte progress, lane scheduling across download/cache/mount/residency with explicit game handoffs; 10 tests over real cache files |
| Buckets compat | crates/ald-buckets | IMPLEMENTED | bucket-1:1 dimensions, default-0, change hooks, lockdown grants, per-bucket density synced into population, dimension agreement proven; 8 tests |
| NUI groundwork | crates/ald-nui | PARTIAL | aldnui:// origins, bounded message queue with counted drops, owner-checked exactly-once callbacks, focus-requires-open-UI, deny-by-default CSP builder; browser embedding BLOCKED_EXTERNAL; 6 tests |
| Input mapping | crates/ald-input | IMPLEMENTED | canonical key catalog, per-resource press bindings, F8 reservation with opt-in, conflict reports in registration order; 6 tests |
| DUI groundwork | crates/ald-dui | PARTIAL | runtime-texture registry with pixel-shape validation, scheme-allowlist navigation, clamped mouse forwarding, bounded page routing; rendering BLOCKED_EXTERNAL; 7 tests |
| Client join driver | client/astryn join.rs | PARTIAL | ordered Handshake→Ready phases on virtual ticks, stall budgets, reject/kick handling, download fractions, terminal stickiness; socket wiring PLANNED; 8 tests |
| Voice groundwork | crates/ald-voice | PARTIAL | proximity/radio/direct channels with bounded membership, proximity tiers, metadata envelopes, mute/deafen routing; audio transport BLOCKED_EXTERNAL; 7 tests |
| Foundation | rust-toolchain.toml, CLI audit, 4 policy docs | IMPLEMENTED | toolchain pinned 1.98.1 (sync verified), `ald dependencies audit-native` gate (3 unit + 1 e2e, core PASS), NATIVE_DEPENDENCY_POLICY/RUST_FIRST_ARCHITECTURE/BUILD_PROFILES/WINDOWS_BUILD |
| Base resources | base-resources/ | IMPLEMENTED | spawn, chat, session, loading, commands, help, devtools; validated manifests and sandboxed Lua entries; e2e validation in cli_e2e.rs |
| Registry scanner | crates/ald-package-scanner | IMPLEMENTED | static scanning, capability auditing, native-extension detection, obfuscation heuristics, reputation authority, revocation, quarantine decision engine; 10 tests |
| AstraSim | client/simulation-client | IMPLEMENTED | headless client simulation, XorShift64 PRNG, lifecycle states, queue drain, resource negotiation, movement, events, packet loss, disconnect/reconnect, dimensions; 10 tests |
| Backup & DR | crates/ald-backup | IMPLEMENTED | full/incremental backups, SHA-256 integrity verification, DB migration safety gates, aldivine.lock serialization & validation, docs/BACKUP_RECOVERY.md; 10 tests |
| Upgrade Planner | crates/ald-upgrade-planner | IMPLEMENTED | topology graph comparison, AstraNet protocol breaks, client mandatory update detection, DB migration steps & destructive checks, rollback limits, restart severity levels, docs/UPGRADE_PLANNER.md; 10 tests |
| Symbol Service & Safe Mode | crates/ald-symbol-service | IMPLEMENTED | minidump/coredump parsing, stack symbolication, deterministic crash fingerprinting, sliding-window crash loop detector, safe mode auto-quarantine, recovery steps; 10 tests |
| Diagnostic Replay | crates/ald-replay | IMPLEMENTED | session recording, tick-accurate playback, tick & event breakpoints, timeline scrubbing, entity state reconstruction; 10 tests |
| ald CLI & Diagnostic Bundle | cli/ald | IMPLEMENTED | scaffold, manifest/config validation, legacy migration reports, native audit gate, `ald report create` sanitized bundles; 17 tests |
| Aegis Node Agent | agent/aegis-node | IMPLEMENTED | server process lifecycle supervision, start/stop/restart, watchdog heartbeat monitor, crash-loop retry caps & quarantine, log ring buffers, build updates & instant rollback, multi-instance isolation; 10 tests |
| Anti-Cheat & Security | crates/ald-security | IMPLEMENTED | secret redaction, speed hack/teleport detection, map boundary bounds, health check, SSRF private/metadata filter, DDoS token buckets, client hash integrity; 12 tests |
| Release Infrastructure | crates/ald-updater | IMPLEMENTED | release channels Stable..Dev, platform artifacts, Ed25519 signature checks, immutable catalog, SBOM checksums, update checker & rollback resolution; 10 tests |
| Localization & Telemetry Consent | crates/ald-telemetry | IMPLEMENTED | default-deny consent model, category gating, string bundle interpolation, accessibility preferences, docs/PRIVACY.md; 9 tests |
| Central HA & Offline Tolerance | crates/ald-ha | IMPLEMENTED | circuit breakers, failover pools, offline grace mode, leader election coordinator; 10 tests |
| Sharding & Handoff | crates/ald-sharding | IMPLEMENTED | spatial region partitioning, atomic 2-phase player handoffs, epoch-fenced distributed entity ownership leases; 10 tests |
| Compatibility Lab | crates/ald-compat-lab | IMPLEMENTED | 21-gate certified support matrix, strict 100% certification evaluation, blocker triage, synthetic & OSS test corpus harness; 10 tests |
| Release Candidate v0.1.0-RC1 | docs/, crates/, apps/ | IMPLEMENTED | all 53 required spec documents written & verified, 971 passing tests, clippy clean, fmt clean, audit-native gate PASS |

## Build Environment

- Host: Windows 11, Rust stable 1.98.1 (x86_64-pc-windows-msvc).
- Visual Studio 2022 Build Tools + Windows 11 SDK (10.0.22621.0) installed.
  Build env (LIB/INCLUDE/PATH) is exported before cargo in this session.
- Full workspace `cargo build` and `cargo test` pass green (971 passed / 0 failed).
- `cargo clippy --workspace --all-targets` passes with 0 warnings.
- `cargo fmt --all -- --check` passes clean.
